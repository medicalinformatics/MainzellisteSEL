Version 1.1-1
	-A field can now be defined but not used for matching (i.e. stored only). 
	 This caused an exception in EpilinkMatcher in former versions.
	-Fixed wrong display of month of birth on the result page (contributed by Daniel Volk) 

Version 1.1
	-Introduced "readPatients", allows retrieving IDAT and IDs of an existing
	 patient, identified by on of his IDs.
	-Tokens are checked for validity upon creation.
	-ID requests with Accept: application/json returns IDs of all types that are
	 specified by data field "idTypes" of the corresponding token.  

Version 1.0
	-Initial release